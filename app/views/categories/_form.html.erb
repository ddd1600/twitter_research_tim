<%= form_for @category do |form| %>
	<fieldset>
		<legend>Category</legend>
			<div>
				<%= form.label :title %>
				<%= form.text_field :title %>
				<%= form.submit %>
				<% end %>
			</div>
		</fieldset>


